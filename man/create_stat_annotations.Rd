% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_helper.R
\name{create_stat_annotations}
\alias{create_stat_annotations}
\title{Create plotly shapes and annotations for statistical test results}
\usage{
create_stat_annotations(
  stats_df,
  fig,
  df,
  x,
  y,
  display = "p.adj",
  hide.ns = FALSE,
  sig.threshold = 0.05,
  line.color = "#000000",
  line.width = 1,
  bracket.style = "capped",
  group.by = NULL,
  facet.by = NULL,
  x.order = NULL,
  font.size = 12,
  step.increase = 0.06,
  text.bump = 0.04,
  bracket.inset = 0.025,
  dodge.width = 1
)
}
\arguments{
\item{stats_df}{Data frame from \code{\link[=compute_pairwise_stats]{compute_pairwise_stats()}}.}

\item{fig}{A plotly figure object. Used to detect subplot axis pairs for
faceted plots.}

\item{df}{The original data frame.}

\item{x}{Character; x-axis column name.}

\item{y}{Character; y-axis column name.}

\item{display}{Character; what to display: \code{"p.adj"}, \code{"p.value"}, or
\code{"symbol"}. Default \code{"p.adj"}.}

\item{hide.ns}{Logical; hide non-significant results. Default FALSE.}

\item{sig.threshold}{Numeric; significance threshold for determining
non-significant results. Default 0.05.}

\item{line.color}{Character; color for bracket lines. Default \code{"#000000"}.}

\item{line.width}{Numeric; width of bracket lines. Default 1.}

\item{bracket.style}{Character; \code{"capped"} for ggpubr-style brackets with
vertical ticks, or \code{"flat"} for a single horizontal line. Default \code{"capped"}.}

\item{group.by}{Character or NULL; nested grouping column.}

\item{facet.by}{Character or NULL; faceting column.}

\item{x.order}{Character vector; order of x-axis categories. If NULL, derived
from unique values of \code{x} column.}

\item{font.size}{Numeric; size of annotation text. Default 12.}

\item{step.increase}{Numeric; fraction of y-range for spacing between
successive brackets. Default 0.06.}

\item{text.bump}{Numeric; fraction of y-range for vertical distance of
text above the bracket line. Default 0.04.}

\item{bracket.inset}{Numeric; fixed amount to inset each bracket endpoint
from the group center position. Creates visual separation between
adjacent brackets at the same y-level. Default 0.025.}

\item{dodge.width}{Numeric; width the \code{group.by} levels at one x category are
dodged across, matching the dodge the plot was built with. Brackets between
two \code{group.by} levels are placed on the same slot centres the boxes sit on,
so this has to be the plot's dodge or they will not line up. Default 1.}
}
\value{
A list with components:
\describe{
\item{annotations}{List of plotly annotation objects.}
\item{shapes}{List of plotly shape objects.}
\item{y.max}{Numeric; maximum y value needed to accommodate all annotations.}
}
}
\description{
Converts results from \code{\link[=compute_pairwise_stats]{compute_pairwise_stats()}} into plotly-compatible
shapes (brackets) and annotations (text labels). Sorts comparisons so that
small-gap brackets are closest to the data and large-gap brackets are higher.
}
\examples{
stats_df <- compute_pairwise_stats(
    df = example_iris,
    x = "Species",
    y = "Sepal.Length",
    test = "wilcox.test"
)

fig <- plotly::plot_ly(
    data = example_iris, x = ~Species, y = ~Sepal.Length, type = "box"
)

stat_result <- create_stat_annotations(
    stats_df = stats_df,
    fig = fig,
    df = example_iris,
    x = "Species",
    y = "Sepal.Length",
    display = "symbol"
)

names(stat_result)

}
\author{
Jared Andrews, Jacob Martin
}
