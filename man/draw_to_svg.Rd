% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figure_export.R
\name{draw_to_svg}
\alias{draw_to_svg}
\title{Render a grid or base drawing to a self-contained SVG fragment}
\usage{
draw_to_svg(draw_fn, width, height, res = 72, id_prefix = NULL, bg = "white")
}
\arguments{
\item{draw_fn}{A function of no arguments that draws onto the active device.}

\item{width, height}{Size in pixels.}

\item{res}{Pixels per inch used to convert \code{width}/\code{height} to the inches the
SVG devices take. The default matches \code{\link[shiny:renderPlot]{shiny::renderPlot()}}'s own \code{res}, so
the fragment is drawn on a canvas the same physical size as the one the
panel was rendered at on screen. That is not cosmetic: a \code{ComplexHeatmap}
legend, its row labels and its titles are all sized in absolute points, so
a canvas even slightly smaller than the on-screen one leaves them the same
size while the heatmap body -- the one flexible element -- absorbs the
entire shortfall. Exporting a legend-heavy heatmap 25\% small squeezed its
cells down to nothing.}

\item{id_prefix}{Optional character scalar used to namespace the fragment's
ids, so several fragments can share one document. See
\code{\link[=.svg_namespace_ids]{.svg_namespace_ids()}}.}

\item{bg}{Background color.}
}
\value{
A character scalar holding an \verb{<svg>} element, or \code{NULL} if the
requested size is not usable or the R build can reach neither SVG device
(no \pkg{svglite} installed and no cairo compiled in). An error raised by
\code{draw_fn} itself (a panel dragged too small to leave any plotting room,
say) propagates to the caller, which is better placed to decide whether to
drop that panel or fail the whole export; the device is closed either way.

The result is a \emph{fragment}: it carries no XML declaration and no \code{xmlns},
because it is meant to be spliced into a document that already declares
them. That makes it unopenable as a file on its own -- writing one out
directly needs the namespace restated first.
}
\description{
Draws onto an SVG device and returns the markup, ready to be placed inside a
larger SVG document. This is what lets a module whose output is not a plotly
graph still contribute vector art to the Figure Builder's figure export: see
the \code{vector_svg} attribute documented in \code{\link[=figureBuilderServer]{figureBuilderServer()}}.
}
\details{
\pkg{svglite} is used when it is installed, because it writes real \verb{<text>}
elements, so labels stay editable in a vector editor. The cairo device
(\code{\link[grDevices:svg]{grDevices::svg()}}) is the fallback: still vector, but it converts text to
glyph paths, which cannot be edited or restyled afterwards.
}
\examples{
svg <- draw_to_svg(function() plot(1:10), width = 480, height = 360)
substr(svg, 1, 24)
}
\seealso{
\code{\link[=draw_to_png]{draw_to_png()}} for the raster counterpart.
}
\author{
Jared Andrews
}
