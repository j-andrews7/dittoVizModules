% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figure_export.R
\name{draw_to_png}
\alias{draw_to_png}
\title{Render a grid or base drawing to PNG bytes}
\usage{
draw_to_png(draw_fn, width, height, res = 72, scale = 2, bg = "white")
}
\arguments{
\item{draw_fn}{A function of no arguments that draws onto the active device.}

\item{width, height}{Size in pixels.}

\item{res}{Pixels per inch used to size the drawing. The default matches
\code{\link[shiny:renderPlot]{shiny::renderPlot()}}'s own \code{res}, so the result is laid out on a canvas the
same physical size as the one the plot was rendered at on screen -- which
matters for anything sized in absolute points; see the note on \code{res} in
\code{\link[=draw_to_svg]{draw_to_svg()}}.}

\item{scale}{Pixel density multiplier. The drawing is laid out at the same
physical size (\code{res} scales with the pixel count) but rasterised at \code{scale}
times the resolution, so the result is a crisp image rather than a
screenshot. Matches the \code{scale} plotly's own image export defaults to.}

\item{bg}{Background color.}
}
\value{
A raw vector holding a PNG file, or \code{NULL} if the requested size is
not usable or the R build has no PNG support. An error raised by \code{draw_fn}
itself propagates to the caller; the device is closed either way.
}
\description{
The raster counterpart to \code{\link[=draw_to_svg]{draw_to_svg()}}, for the same job: letting a module
whose output is not a plotly graph supply its own artwork to an export. Where
\code{\link[=draw_to_svg]{draw_to_svg()}} yields markup to splice, this yields the bytes of a finished
file.
}
\examples{
png_bytes <- draw_to_png(function() plot(1:10), width = 480, height = 360)
head(png_bytes, 4)
}
\seealso{
\code{\link[=draw_to_svg]{draw_to_svg()}} for the vector counterpart.
}
\author{
Jared Andrews
}
