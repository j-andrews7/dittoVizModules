% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_helper.R
\name{stat_bracket_y_max}
\alias{stat_bracket_y_max}
\title{Y-axis top needed to draw statistical annotation brackets in full}
\usage{
stat_bracket_y_max(
  df,
  x,
  y,
  pairs = NULL,
  group.by = NULL,
  facet.by = NULL,
  per.facet = TRUE,
  step.increase = 0.06,
  text.bump = 0.04,
  bracket.inset = 0.025,
  hide.ns = FALSE,
  sig.threshold = 0.05,
  test = "wilcox.test",
  p.adjust.method = "holm",
  paired = FALSE,
  dodge.width = 1
)
}
\arguments{
\item{df}{Data frame the statistics are computed on. For a module that
reshapes its data for testing (e.g. a multi-variable Y selection), pass the
reshaped frame, not the raw one.}

\item{x}{Character; x-axis column name.}

\item{y}{Character; y-axis column name(s). Several may be given, in which
case the data range spans all of them.}

\item{pairs}{List of length-2 character vectors, or \code{NULL} for all pairwise
combinations.}

\item{group.by}{Character or \code{NULL}; nested grouping column.}

\item{facet.by}{Character or \code{NULL}; faceting column.}

\item{per.facet}{Logical; whether tests are run within each facet.}

\item{step.increase}{Numeric; fraction of the y-range between successive
bracket levels. Default 0.06.}

\item{text.bump}{Numeric; fraction of the y-range between a bracket and its
label. Default 0.04.}

\item{bracket.inset}{Numeric; endpoint inset, which affects how tightly
brackets pack onto a level. Default 0.025.}

\item{hide.ns}{Logical; whether non-significant brackets are dropped before
drawing. When \code{TRUE} the tests are run so only the surviving comparisons
are counted. Default \code{FALSE}.}

\item{sig.threshold}{Numeric; significance cutoff used with \code{hide.ns}.}

\item{test, p.adjust.method, paired}{Passed to \code{\link[=compute_pairwise_stats]{compute_pairwise_stats()}}, and
used only when \code{hide.ns} is \code{TRUE}. Match them to the render's settings, or
the wrong comparisons are counted.}

\item{dodge.width}{Numeric; width the \code{group.by} levels at one x category are
dodged across. Match the plot's, or the brackets will not line up with the
boxes. Default 1.}
}
\value{
A single number giving the y-axis maximum the brackets need, or
\code{NULL} when nothing would be drawn.
}
\description{
Works out how high the significance brackets from \code{\link[=create_stat_annotations]{create_stat_annotations()}}
will reach, so an axis range can reserve room for them up front rather than
having the plot drawn with the brackets clipped or pushed outside the panel.
}
\details{
The brackets are stacked above the data: each packing level sits
\code{step.increase} of the data range above the last, the label sits \code{text.bump}
above its bracket, and a final \code{step.increase} of clearance is left at the
top. Which comparisons land on which level is decided by
\code{.assign_bracket_levels()}, shared with the drawing code, so the two agree
exactly.

Which comparisons there are to place depends only on the grouping columns and
the user's pair selection, so no test needs to be run — except under
\code{hide.ns = TRUE}, where the non-significant brackets are dropped before
packing and the tests have to be run to know which those are. That case
repeats the work \code{\link[=compute_pairwise_stats]{compute_pairwise_stats()}} does at render time; it is
skipped for several \code{y} columns at once, where the comparisons no longer map
one-to-one onto a single test run, and the result is then an upper bound.

\code{\link[=apply_stat_annotations]{apply_stat_annotations()}} still has the last word on the drawn range, so a
bracket is never clipped even where this over- or under-estimates.
}
\examples{
# Three species means three comparisons, which stack onto two levels.
stat_bracket_y_max(example_iris, x = "Species", y = "Sepal.Length")

# Compare against the raw data maximum.
max(example_iris$Sepal.Length)
}
\seealso{
\code{\link[=create_stat_annotations]{create_stat_annotations()}}, \code{\link[=apply_stat_annotations]{apply_stat_annotations()}}
}
\author{
Jared Andrews
}
